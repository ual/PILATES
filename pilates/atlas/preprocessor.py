import os
import logging
import gzip
import shutil
import pandas as pd

logger = logging.getLogger(__name__)


def get_data_inplace()

    # TBD, use this preprocessor to copy/move data from previous step into the input data folder 
    # eg see pilates/beam/preprocessor.py

    return
